# Created by Vince Chang
# The purpuse of this file is to make sure that the code I have in
# src/main/java/com/example/bubba/TheServiceImpl.java -> @Value("${message}
# which results to application.properties, overwrites the message from my
# bubba.properties on my github!

# Name of the application & also using configsvr(application) to host

spring.application.name=bubba
spring.cloud.config.uri=http://localhost:8888